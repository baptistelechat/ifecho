---
id: LRN-060
type: learning
date: 2026-06-27
tags: [useRef, cache, hmr, vite, debugging, vigilance]
---

# LRN-060 — `useRef` cache 30min persiste via HMR Vite → hard reload nécessaire

| Pattern observé                                                                                                       | Contexte                                                                                                                                        | Application future                                                                                                                                             |
| --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HMR rafraîchit la vue sans recharger le module — `cacheRef.current` garde ses données entre les modifications de code | Fix dans `useVigilanceData.ts` : nouvelles règles de filtre non appliquées tant que le cache 30min existait, sans que l'utilisateur ne le sache | Annoncer systématiquement `Ctrl+Shift+R` (hard reload) après tout fix sur un hook avec cache `useRef` ; distinguer ce cas du cache Vite (`node_modules/.vite`) |
