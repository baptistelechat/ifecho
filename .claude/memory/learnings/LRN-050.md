---
id: LRN-050
type: learning
date: 2026-06-23
tags:
  [
    react,
    useRef,
    stale-closure,
    event-handler,
    useEffect,
    state-read,
    click-outside,
  ]
---

# LRN-050 — `valueRef.current = value` dans le render : lire le state courant dans un handler stable

| Pattern observé                                                                                                                                                                                                                                                                                                                                              | Contexte                                                                                                                                                                                                                                  | Application future                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `const queryRef = useRef(query); queryRef.current = query;` dans le corps du composant (pas dans un `useEffect`). Le handler défini dans `useEffect([])` lit `queryRef.current` et obtient toujours la valeur à jour, sans que `query` soit dans les dépendances. La mise à jour se fait à chaque render, garantissant la fraîcheur sans recréer le handler. | `handleClickOutside` dans `LocationSearch` (ifecho) : besoin de `query.length` dans un listener mousedown one-shot, sans recréer le listener à chaque frappe ni ajouter `query` en dep (ce qui recréerait le handler à chaque caractère). | Quand un handler stable (`useEffect([])`) doit lire le dernier state → `valueRef.current = value` dans le render. Distinct de [LRN-027](LRN-027.md) (hook retournant objet instable) : ici c'est un scalar state, pas un objet retourné par un hook. |

## Références

- [LRN-027](LRN-027.md) — Hook retournant objet instable → event-handler ref
