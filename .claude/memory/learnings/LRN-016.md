---
id: LRN-016
type: learning
date: 2026-06-21
tags:
  [open-meteo, forecast-days, sliding-window, midnight, filter, sunrise, sunset]
---

# LRN-016 — `forecast_days: "2"` + filtre `[now, now+24h]` pour traverser minuit

| Pattern observé                                                                                                                                                                                                                        | Contexte                                                                           | Application future                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| Fenêtre 24h traversant minuit nécessite 48 entrées (J et J+1). Filtre : `date === nowDate && hour >= nowHour` OU `date > nowDate && hour < nowHour`. Exposer `sunrise2`/`sunset2` depuis `daily[1]` pour `isNightHour` correct sur J+1 | VentilationTimeline et IdealSlots passés de journée calendaire à fenêtre glissante | Tout appel Open-Meteo avec fenêtre traversant minuit : passer à `forecast_days: "2"` et adapter le filtre d'extraction |

## Références

- [BDR-023](../decisions/BDR-023.md) — décision architecture sliding window
