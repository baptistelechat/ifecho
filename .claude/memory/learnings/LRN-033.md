---
id: LRN-033
type: learning
date: 2026-06-22
tags:
  [
    framer-motion,
    AnimatePresence,
    height-auto,
    overflow-hidden,
    flat-structure,
    item-animation,
    list,
    react,
  ]
---

# LRN-033 — `height: 0 → "auto"` + flat `AnimatePresence initial={false}` = animation hauteur par item

| Pattern observé                                                                                                                                                                                                                                                                                                 | Contexte                                                                                                                    | Application future                                                                                                                                                                    |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Structure plate dans `AnimatePresence initial={false}` — chaque item (incluant l'état vide) a `initial={{ height: 0 }}` / `animate={{ height: "auto" }}` / `exit={{ height: 0 }}` + `className="overflow-hidden"`. Pas de wrapper externe sur l'ensemble. La card parent s'étire naturellement avec ses enfants | IdealSlots — slots favorables ajoutés/supprimés selon la météo ; remplace un wrapper `mode="wait"` qui causait snap + flash | Toute liste dont les items apparaissent/disparaissent sans flash ni snap brutal ; l'état vide doit lui aussi être un enfant direct de `AnimatePresence` avec la même animation height |
