---
id: LRN-032
type: learning
date: 2026-06-22
tags:
  [
    framer-motion,
    stagger,
    staggerChildren,
    LazyMotion,
    domAnimation,
    delay,
    animation,
    react,
  ]
---

# LRN-032 — `staggerChildren` + delays explicites silencieux dans LazyMotion

| Pattern observé                                                                                                                                                                    | Contexte                                                                                                                                                    | Application future                                                                                                   |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `staggerChildren` dans `variants` ET delays explicites sur chaque `m.div` produisent le même résultat visuel : toutes les cards apparaissent simultanément — aucun stagger visible | ifecho — App.tsx avec `LazyMotion features={domAnimation}` ; 3 approches tentées (staggerChildren, shared constant delay, inline delay) toutes silencieuses | Si stagger requis avec LazyMotion, tester `domMax` au lieu de `domAnimation` ; sinon préférer un slide global unique |

## Références

- [BDR-031](../decisions/BDR-031.md) — slide global comme contournement
